[project]
name = "discourse-archive-tools"
version = "0.1.0"
description = "Read-only Discourse archiving helpers for personal authenticated archives."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cryptography>=42",
]

[tool.uv]
package = false
